# Ignore vscode files
.vscode/

# Ignore macOS system files
**/.DS_Store

# Ignore the real per-domain pull-server defaults (holds registration keys).
# Only the .sample template is tracked.
scripts/pull/CfgLcmPull.DomainDefaults.psd1
